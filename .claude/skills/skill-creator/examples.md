# Real Skills Examples

> Реальные примеры Claude Skills с анализом структуры и решений.

---

## Пример 1: Простой Skill — project-bash-helper

### Структура

```
project-bash-helper/
└── SKILL.md
```

**Тип:** Простой skill
**Файлов:** 1

### Ключевые секции SKILL.md (с комментариями)

```markdown
---
name: project-bash-helper
description: Expert in running bash commands and scripts for this project. Knows project structure, correct absolute paths, virtual environment setup. Activates when executing bash commands, running scripts, or performing file operations that require shell execution. Use triggers like "run script", "execute", "bash command".
# 👆 Отличный description: ЧТО делает + КОГДА активировать + триггеры
allowed-tools: Bash, Read, Glob
# 👆 Ограничение безопасности: только чтение и bash, без редактирования
---

# Project Bash Expert

Эксперт для выполнения bash команд в контексте этого проекта.

## 🚫 КРИТИЧЕСКОЕ ПРАВИЛО: Навигация

**НИКОГДА НЕ ИСПОЛЬЗУЙ `cd` КОМАНДЫ!**

Рабочая директория: `/path/to/project`

### ❌ Неправильно:

```bash
cd scripts && python script.py
```

### ✅ Правильно:

```bash
python /path/to/project/scripts/script.py
```

**Причина:** Рабочая директория должна оставаться стабильной.

---

## 🐍 Virtual Environment

**Virtual environment путь:** `/path/to/project/.venv`

### Активация перед запуском:

```bash
source /path/to/project/.venv/bin/activate && \\
python /path/to/project/scripts/script.py
```

---

## 📁 Структура проекта

### Ключевые директории:

```
/path/to/project/
├── scripts/             # Исполняемые скрипты
├── .claude/
│   ├── commands/        # Slash команды
│   └── skills/          # Skills
├── src/                 # Исходный код
└── .venv/               # Virtual environment
```

---

## 🎯 Когда активироваться

Этот skill активируется когда пользователь:

- Просит запустить скрипт из проекта
- Хочет выполнить bash команду
- Работает с файловой системой через shell

---

## 🛡️ Безопасность

**allowed-tools:** `Bash, Read, Glob`

Этот skill может:
- ✅ Выполнять bash команды
- ✅ Читать файлы

Этот skill НЕ может:
- ❌ Редактировать файлы напрямую
```

### Анализ структуры

**Почему простой skill (только SKILL.md)?**

✅ **Инструкции компактные:** Всё помещается в один файл
✅ **Нет дополнительных ресурсов:** Не нужны шаблоны или скрипты
✅ **Линейный workflow:** Простые правила использования bash

**Что делает его хорошим?**

✅ **Отличный description:**
   - Ясно описывает возможности
   - Содержит конкретные триггеры ("run script", "execute")
   - Упоминает когда активировать

✅ **Структурированные инструкции:**
   - Критическое правило выделено (НИКОГДА НЕ cd)
   - Конкретные примеры (правильно/неправильно)
   - Структура проекта документирована

✅ **Security ограничения:**
   - allowed-tools явно перечислены
   - Ясно что можно и нельзя

**Использование Progressive Disclosure:**
- **Level 1:** Metadata в frontmatter (~150 токенов)
- **Level 2:** Весь SKILL.md (~800 токенов)
- **Level 3+:** Нет дополнительных файлов

---

## Пример 2: Сложный Skill — document-processor

### Структура

```
document-processor/
├── SKILL.md                    # Главный workflow
├── reference.md                # Детальная спецификация формата
└── templates/
    ├── basic_template.md       # Базовый шаблон
    ├── advanced_template.md    # Расширенный шаблон
    └── summary_template.md     # Шаблон для резюме
```

**Тип:** Сложный skill
**Файлов:** 5

### Ключевые секции SKILL.md (с комментариями)

```markdown
---
name: document-processor
description: Process documents into structured format with proper metadata and templates. Activates when user provides documents, mentions "process document", "format document", or wants to create structured content. Use when seeing document files or document processing requests.
# 👆 Конкретные триггеры + patterns + ключевые слова
allowed-tools: Read, Write, Bash, Grep, Glob
# 👆 Больше инструментов для сложного workflow
---

# Document Processor

Эксперт по обработке документов и созданию структурированного контента.

## 🎯 Когда активироваться

Этот skill активируется когда пользователь:

- Предоставляет документ для обработки
- Просит обработать документ ("process document")
- Хочет создать структурированный контент
- Упоминает "format document", "structure content"

**Триггеры:** "document", "process", "format", "structure"

---

## 📋 Общий Workflow

### Фаза 1: Извлечение контента

1. Получить входной документ
2. Извлечь метаданные
3. Проанализировать структуру

### Фаза 2: Анализ метаданных

Извлечь из документа:
- title, author, date, source

### Фаза 3: Определение типа обработки

- **basic** — простая обработка
- **advanced** — расширенная обработка с анализом
- **summary** — создание резюме

### Фаза 4: Обработка по типу

#### Режим: basic
1. Применить базовые правила
2. Создать frontmatter используя `templates/basic_template.md`
   # 👆 Ссылка на отдельный файл для progressive disclosure

#### Режим: advanced
1. Глубокий анализ структуры
2. Frontmatter используя `templates/advanced_template.md`

### Фаза 5: Генерация имени файла

**Правила из reference.md для деталей:**
# 👆 Отсылка к reference.md для детальных правил

- **basic:** `doc_<название>.md`
- **advanced:** `doc_advanced_<название>.md`

### Фаза 6: Поиск связанных документов

Используй Grep для поиска:
```
Grep pattern="<ключевое слово>" path="docs/"
```

### Фаза 7: Сохранение

Создать файл и сообщить пользователю детали.

---

## 📚 Reference Files

Для детальных правил см.:

- `reference.md` — полная спецификация формата
  # 👆 Загружается только когда нужны детали правил

- `templates/basic_template.md` — базовый шаблон
- `templates/advanced_template.md` — расширенный шаблон
- `templates/summary_template.md` — шаблон резюме

**Загружай reference.md** когда нужны детали правил или примеры.
# 👆 Явная инструкция когда загружать

---

## 🛡️ Безопасность

**allowed-tools:** `Read, Write, Bash, Grep, Glob`

Этот skill может:
- ✅ Читать документы
- ✅ Создавать новые файлы
- ✅ Искать связанные документы

Этот skill НЕ может:
- ❌ Редактировать существующие документы без подтверждения
- ❌ Удалять файлы

---

## 🎨 Best Practices

1. Всегда используй уникальные имена файлов
2. Сохраняй оригиналы для отладки
3. Определяй метаданные автоматически
4. Ищи связанные документы перед сохранением
5. Следуй спецификации строго (см. reference.md)
```

### Анализ структуры

**Почему сложный skill?**

✅ **Множество компонентов:** 3 режима обработки (basic/advanced/summary)
✅ **Шаблоны:** Отдельные templates для каждого типа
✅ **Детальные правила:** reference.md с полной спецификацией

**Progressive Disclosure в действии:**

- **Level 1:** Metadata (~200 токенов)
  ```yaml
  name: document-processor
  description: Process documents... [триггеры]
  ```

- **Level 2:** SKILL.md (~2000 токенов)
  - Общий workflow
  - Ссылки на reference.md
  - Ссылки на templates

- **Level 3:** reference.md (~3000 токенов)
  - Загружается только когда нужны детальные правила
  - Полная спецификация формата

- **Level 4:** templates/ (по ~200 токенов каждый)
  - Загружаются только при создании конкретного типа документа

**Итого:** Без активации skill потребляет всего ~200 токенов. При активации базовый workflow ~2200 токенов. Детали загружаются только по требованию.

**Что делает его хорошим?**

✅ **Умное разделение контекста:**
   - Базовый workflow в SKILL.md
   - Детальные правила в reference.md
   - Шаблоны в отдельных файлах

✅ **Явные ссылки на файлы:**
   - "см. reference.md для деталей"
   - "используя templates/basic_template.md"
   - Claude знает когда что загружать

✅ **Многорежимность:**
   - Три типа обработки
   - Каждый с отдельным шаблоном
   - Взаимоисключающие контексты раздельны

---

## Сравнение структур

### Простой vs Сложный

| Аспект | project-bash-helper (простой) | document-processor (сложный) |
|--------|-------------------------------|------------------------------|
| **Файлов** | 1 (SKILL.md) | 5 (SKILL.md + reference + 3 templates) |
| **Токенов Level 1** | ~150 | ~200 |
| **Токенов Level 2** | ~800 (всё) | ~2000 (основной workflow) |
| **Токенов Level 3+** | 0 | ~3500 (reference + templates) |
| **Компонентов** | Простые правила | 3 режима, шаблоны, генерация |
| **Progressive Disclosure** | 2 уровня | 4+ уровня |

**Вывод:**
- **Простой skill:** Когда всё помещается в 1-2 страницы
- **Сложный skill:** Когда есть множество компонентов и взаимоисключающие контексты

---

## Варианты структур директорий

### Минимальная (простой skill)

```
skill-name/
└── SKILL.md
```

**Когда использовать:** Простые инструкции, нет дополнительных ресурсов

**Примеры use cases:**
- Bash command helper
- Code formatter
- Simple validator

---

### С reference (средний skill)

```
skill-name/
├── SKILL.md        # Основной workflow
└── reference.md    # Детальная справка
```

**Когда использовать:** Есть детальная документация, которая нужна не всегда

**Примеры use cases:**
- API client (reference.md с API spec)
- Database helper (reference.md с schema)
- Configuration manager (reference.md с опциями)

---

### С templates (средний-сложный)

```
skill-name/
├── SKILL.md
└── templates/
    ├── template1.md
    └── template2.md
```

**Когда использовать:** Skill генерирует контент по шаблонам

**Примеры use cases:**
- Document generator
- Code scaffolder
- Report creator

---

### Полная структура (сложный skill)

```
skill-name/
├── SKILL.md              # Главный workflow
├── reference.md          # Детальная документация
├── templates/            # Шаблоны для генерации
│   ├── template1.md
│   └── template2.md
├── scripts/              # Исполняемый код (опционально)
│   └── helper.py
└── examples/             # Примеры (опционально)
    └── example1.md
```

**Когда использовать:** Множество компонентов, шаблоны, код

**Примеры use cases:**
- Complex automation
- Multi-step processing
- Document pipeline

---

## Ключевые паттерны из примеров

### 1. Description с триггерами

**project-bash-helper:**
```yaml
description: ... Activates when executing bash commands, running scripts, or performing file operations that require shell execution. Use triggers like "run script", "execute", "bash command".
```

**document-processor:**
```yaml
description: ... Activates when user provides documents, mentions "process document", "format document", or wants to create structured content.
```

**Паттерн:** `[Возможности]. Activates when [сценарии] or [when user mentions] [ключевые слова].`

### 2. Явные ссылки на файлы

```markdown
## Фаза 4: Обработка

Создать frontmatter используя `templates/basic_template.md`:
```

```markdown
## Reference Files

- `reference.md` — полная спецификация формата

**Загружай reference.md** когда нужны детали правил.
```

**Паттерн:** Явно говори Claude когда загружать дополнительные файлы.

### 3. Security секция

Всегда документируй:
- allowed-tools
- Что skill МОЖЕТ
- Что skill НЕ МОЖЕТ

### 4. Структурированные инструкции

- Используй секции с emoji (🎯, 📋, 🛡️)
- Разбивай на шаги
- Примеры "правильно/неправильно"

---

**Эти примеры показывают правильную структуру skills. Адаптируй их под свой проект!**
